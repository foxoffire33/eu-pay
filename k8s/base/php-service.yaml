apiVersion: v1
kind: Service
metadata:
  name: eupay-php
  namespace: eupay
  labels:
    app: eupay-php
spec:
  selector:
    app: eupay-php
  ports:
    - port: 9000
      targetPort: 9000
      protocol: TCP
  type: ClusterIP
